"""
Request handlers.

One module per resource. A handler reads validated input, calls a service, and
shapes the answer; it does not decide its own URL and it does not talk to the
database engine directly.
"""
