"""
The work itself.

Services hold the behaviour that would otherwise pile up in handlers: loading
the model, scoring, reading and checking uploaded files, running jobs and
training. Nothing here knows about HTTP.
"""
